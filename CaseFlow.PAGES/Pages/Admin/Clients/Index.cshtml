@page
@model CaseFlow.PAGES.Pages.Admin.Clients.IndexModel
@{
    ViewData["Title"] = "Клієнти";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div class="d-flex gap-2">
        <button class="btn btn-outline-secondary filter-btn" data-table="clients-table">
            <i class="fas fa-filter me-1"></i>Фільтр
        </button>
        <a asp-page="Create" class="btn btn-primary">Створити нового клієнта</a>
    </div>
</div>

<table id="clients-table" class="table sortable-table paginated-table">
    <thead>
    <tr>
        <th class="text-center">ID</th>
        <th class="text-center">Ім'я</th>
        <th class="text-center">Email</th>
        <th class="text-center">Телефон</th>
        <th class="text-center">Місто</th>
        <th class="text-center">Зареєстровано</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var c in Model.Clients)
    {
        <tr class="clickable-row" data-href="@Url.Page("Details", new { id = c.Id })">
            <td>@c.Id</td>
            <td>@c.FullName</td>
            <td>@c.Email</td>
            <td>@c.PhoneNumber</td>
            <td>@c.City</td>
            <td>@c.RegistrationDate</td>
        </tr>
    }
    </tbody>
    </table>

    <!-- Server-side Pagination -->
    @if (Model.PagedClients.TotalPages > 1)
    {
        <nav aria-label="Clients pagination" class="mt-3">
            <ul class="pagination justify-content-center">
                @if (Model.PagedClients.HasPreviousPage)
                {
                    <li class="page-item">
                        <a class="page-link" href="?pageNumber=@(Model.CurrentPage - 1)">Попередня</a>
                    </li>
                }
                
                @for (int i = Math.Max(1, Model.CurrentPage - 2); i <= Math.Min(Model.PagedClients.TotalPages, Model.CurrentPage + 2); i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" href="?pageNumber=@i">@i</a>
                    </li>
                }
                
                @if (Model.PagedClients.HasNextPage)
                {
                    <li class="page-item">
                        <a class="page-link" href="?pageNumber=@(Model.CurrentPage + 1)">Наступна</a>
                    </li>
                }
            </ul>
        </nav>
        
        <div class="text-center text-muted">
            Показано @((Model.CurrentPage - 1) * Model.PageSize + 1)-@(Math.Min(Model.CurrentPage * Model.PageSize, Model.PagedClients.TotalCount)) з @Model.PagedClients.TotalCount записів
        </div>
    }

<div class="mb-5"></div>

@section Scripts {
    <script src="~/js/table-sorting.js"></script>
    <script src="~/js/table-filters.js"></script>
    <script src="~/js/table-pagination.js"></script>
}

