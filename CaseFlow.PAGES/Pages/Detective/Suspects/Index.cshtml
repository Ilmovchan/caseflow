@page
@model CaseFlow.PAGES.Pages.Detective.Suspects.IndexModel
@{
    ViewData["Title"] = "Підозрювані";
}

<div class="d-flex justify-content-between align-items-center mb-3 gap-2">
    <form method="get" class="d-flex gap-3" style="flex: 1; max-width: 400px;">
        <input type="text" name="searchTerm" class="form-control" placeholder="Пошук..." value="@Model.SearchTerm" />
        <button type="submit" class="btn btn-outline-secondary" title="Пошук">
            <i class="fas fa-search"></i>
        </button>
        @if (!string.IsNullOrWhiteSpace(Model.SearchTerm))
        {
            <a href="@Url.Page("Index")" class="btn btn-outline-secondary" title="Очистити">
                <i class="fas fa-times"></i>
            </a>
        }
    </form>
</div>

<table id="suspects-table" class="table sortable-table paginated-table">
    <thead>
    <tr>
        <th class="text-center">ID</th>
        <th class="text-center">ПІБ</th>
        <th class="text-center">Псевдонім</th>
        <th class="text-center">Телефон</th>
        <th class="text-center">Дата народження</th>
        <th class="text-center">Місто</th>
        <th class="text-center">Статус</th>
        <th class="text-center">Дії</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var s in Model.Suspects)
    {
        <tr class="clickable-row" data-href="@Url.Page("Details", new { id = s.Id })">
            <td>@s.Id</td>
            <td>@($"{s.LastName} {s.FirstName} {s.FatherName}".Trim())</td>
            <td>@(s.Nickname ?? "-")</td>
            <td>@(s.PhoneNumber ?? "-")</td>
            <td>@(s.DateOfBirth?.ToString() ?? "-")</td>
            <td>@(s.City ?? "-")</td>
            <td>
                <span class="badge bg-@(s.ApprovalStatus.ToString() == "Approved" ? "success" : s.ApprovalStatus.ToString() == "Pending" ? "warning" : s.ApprovalStatus.ToString() == "Declined" ? "danger" : "secondary") approval-status" data-id="@s.Id" data-status="@s.ApprovalStatus">
                    @(s.ApprovalStatus.ToString() == "Approved" ? "Схвалено" : s.ApprovalStatus.ToString() == "Pending" ? "Очікує" : s.ApprovalStatus.ToString() == "Declined" ? "Відхилено" : "Чернетка")
                </span>
            </td>
            <td class="approval-actions">
                @if (s.ApprovalStatus.ToString() == "Draft")
                {
                    <button class="btn btn-sm btn-approve" data-id="@s.Id" data-type="suspect" title="Надіслати на перевірку">
                        <i class="fas fa-check" style="color: #0dcaf0;"></i>
                    </button>
                }
                else if (s.ApprovalStatus.ToString() == "Declined")
                {
                    <span class="text-muted">Відхилено</span>
                }
                else if (s.ApprovalStatus.ToString() == "Approved")
                {
                    <span class="text-muted">Оброблено</span>
                }
                else if (s.ApprovalStatus.ToString() == "Pending")
                {
                    <span class="text-muted">Очікує перевірки</span>
                }
            </td>
        </tr>
    }
    </tbody>
</table>

<!-- Server-side Pagination -->
@if (Model.PagedSuspects.TotalPages > 1)
{
    <nav aria-label="Suspects pagination" class="mt-3">
        <ul class="pagination justify-content-center">
            @if (Model.PagedSuspects.HasPreviousPage)
            {
                <li class="page-item">
                    <a class="page-link" href="?pageNumber=@(Model.CurrentPage - 1)@(string.IsNullOrWhiteSpace(Model.SearchTerm) ? "" : $"&searchTerm={Uri.EscapeDataString(Model.SearchTerm)}")">Назад</a>
                </li>
            }

            @for (int i = Math.Max(1, Model.CurrentPage - 1); i <= Math.Min(Model.PagedSuspects.TotalPages, Model.CurrentPage + 1); i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                    <a class="page-link" href="?pageNumber=@i@(string.IsNullOrWhiteSpace(Model.SearchTerm) ? "" : $"&searchTerm={Uri.EscapeDataString(Model.SearchTerm)}")">@i</a>
                </li>
            }

            @if (Model.PagedSuspects.HasNextPage)
            {
                <li class="page-item">
                    <a class="page-link" href="?pageNumber=@(Model.CurrentPage + 1)@(string.IsNullOrWhiteSpace(Model.SearchTerm) ? "" : $"&searchTerm={Uri.EscapeDataString(Model.SearchTerm)}")">Далі</a>
                </li>
            }
        </ul>
    </nav>

    <div class="text-center text-muted">
        Показано @((Model.CurrentPage - 1) * Model.PageSize + 1)-@(Math.Min(Model.CurrentPage * Model.PageSize, Model.PagedSuspects.TotalCount)) з @Model.PagedSuspects.TotalCount записів
    </div>
}

<div class="mb-5"></div>

@section Scripts {
    <script src="~/js/table-sorting.js"></script>
    <script src="~/js/table-filters.js"></script>
    <script src="~/js/table-pagination.js"></script>
    <script src="~/js/approval-system.js"></script>
}

